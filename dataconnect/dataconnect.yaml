specVersion: "v1"
serviceId: "chessreforged"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chessreforged-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
